# Advanced Deep Learning in Physics and Engineering

Course repository for assignments and experiments.

## Structure

- `notebooks/` — assignment notebooks
- `scripts/` — helper scripts
- `data/` — local dataset files (not tracked)
- `runs/` — local outputs, logs, checkpoints (not tracked)

## Environment

This project uses Python in a virtual environment.

Install dependencies with:

```bash
pip install -r requirements.txt
```

Large data files, virtual environments, and training outputs are excluded from git.